# Performance Measuring
Simple performance measuring tool.

## Setup
- Add the mod found in the `log-producer` folder
- Copy `log-collector/log-collector.exe` into your Victoria 3 logs directory
- Start the game (Main Menu)
- Start the `log-collector.exe`
- Start an observer game
- Results can be found in the `performance` directory which was created in the Victoria 3 logs directory